import { Logout } from "@/components/auth/Logout";

const ChatAppPage = () => {
    return (
        <div>
            <h1>Chat App</h1>
            <Logout />
        </div>
    );
};

export default ChatAppPage;